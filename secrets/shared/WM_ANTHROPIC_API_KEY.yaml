# WM_ANTHROPIC_API_KEY.yaml — Anthropic cloud brain API key (TEMPLATE, NOT YET ENCRYPTED)
#
# Encrypt with: sops --encrypt --in-place secrets/shared/WM_ANTHROPIC_API_KEY.yaml
#
# Expected plaintext:
#   value: "sk-ant-api03-<REDACTED>"
#
# PLACEHOLDER — replace this entire file with sops-encrypted output.
value: "PLACEHOLDER — encrypt with sops before use"
description: "WM_ANTHROPIC_API_KEY — Anthropic API key for the cloud brain tier"
role: shared
created: "2026-06-09"
# sops: {}  # will be populated by: sops --encrypt --in-place
