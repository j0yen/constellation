# tailscale_authkey.yaml — laptop role pre-auth enrollment key (TEMPLATE, NOT YET ENCRYPTED)
#
# Encrypt with: sops --encrypt --in-place secrets/laptop/tailscale_authkey.yaml
#
# Expected plaintext:
#   value: "tskey-auth-<REDACTED>"
#
# NOTE: This secret is encrypted ONLY for the laptop role key.
# The cloud role CANNOT decrypt this file (AC4).
#
# PLACEHOLDER — replace this entire file with sops-encrypted output.
value: "PLACEHOLDER — encrypt with sops before use"
description: "Tailscale/Headscale pre-auth enrollment key for laptop role"
role: laptop
created: "2026-06-09"
# sops: {}  # will be populated by: sops --encrypt --in-place
