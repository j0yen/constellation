# tailscale_authkey.yaml — cloud role pre-auth enrollment key (TEMPLATE, NOT YET ENCRYPTED)
#
# Encrypt with: sops --encrypt --in-place secrets/cloud/tailscale_authkey.yaml
#
# Expected plaintext:
#   value: "tskey-auth-<REDACTED>"
#
# NOTE: This secret is encrypted ONLY for the cloud role key.
# The laptop role CANNOT decrypt this file (AC4).
#
# PLACEHOLDER — replace this entire file with sops-encrypted output.
value: "PLACEHOLDER — encrypt with sops before use"
description: "Tailscale/Headscale pre-auth enrollment key for cloud role"
role: cloud
created: "2026-06-09"
# sops: {}  # will be populated by: sops --encrypt --in-place
