# nats.creds.yaml — cloud NATS hub credentials (TEMPLATE, NOT YET ENCRYPTED)
#
# Encrypt with: sops --encrypt --in-place secrets/cloud/nats.creds.yaml
#
# This is the cloud-node-specific hub operator credentials.
# Distinct from shared/nats.creds.yaml which holds leaf node credentials.
#
# PLACEHOLDER — replace this entire file with sops-encrypted output.
value: "PLACEHOLDER — encrypt with sops before use"
description: "NATS hub operator credentials for the cloud constellation node"
role: cloud
created: "2026-06-09"
# sops: {}  # will be populated by: sops --encrypt --in-place
