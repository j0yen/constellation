# constellation fleet node map
# Format: role <TAB> hostname <TAB> description
# hostname = the short MagicDNS name assigned to each node (set on enrollment).
# Edit these to match your actual Tailscale hostnames.
# Lines beginning with '#' are comments.
cloud	hub	cloud VPS — NATS hub, exit node
desktop	forge	home desktop — GPU / compile node
laptop	nomad	roaming dev laptop
